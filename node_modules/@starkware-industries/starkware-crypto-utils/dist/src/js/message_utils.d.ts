import BN from 'bn.js';
declare function assertInRange(input: BN, lowerBound: BN, upperBound: BN, inputName?: string): void;
declare class Range {
    lowerBound: BN;
    upperBound: BN;
    constructor(lowerBound: BN, upperBound: BN);
}
declare function assertInMultiRange(input: BN, ranges: Range[], inputName?: string): void;
export { Range, assertInRange, assertInMultiRange };
